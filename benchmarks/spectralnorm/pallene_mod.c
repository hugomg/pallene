/* This file was generated by the Pallene compiler. Do not edit by hand" */
/* Indentation and formatting courtesy of pallene/C.lua */

#include "pallene_core.h"

/* ------- */
/* Records */
/* ------- */

/* ------------------- */
/* Function Prototypes */
/* ------------------- */

static void function_01(
    lua_State *L,
    Udata *G,
    StackValue *base
);
static lua_Number function_02(
    lua_State *L,
    Udata *G,
    StackValue *base,
    lua_Integer x1, /* i */
    lua_Integer x2  /* j */
);
static void function_03(
    lua_State *L,
    Udata *G,
    StackValue *base,
    lua_Integer x1, /* N */
    Table *x2, /* v */
    Table *x3  /* out */
);
static void function_04(
    lua_State *L,
    Udata *G,
    StackValue *base,
    lua_Integer x1, /* N */
    Table *x2, /* v */
    Table *x3  /* out */
);
static void function_05(
    lua_State *L,
    Udata *G,
    StackValue *base,
    lua_Integer x1, /* N */
    Table *x2, /* v */
    Table *x3  /* out */
);
static lua_Number function_06(
    lua_State *L,
    Udata *G,
    StackValue *base,
    lua_Integer x1  /* N */
);

/* ------------------------ */
/* Function Implementations */
/* ------------------------ */

/* $init */
static void function_01(
    lua_State *L,
    Udata *G,
    StackValue *base
) {
    ptrdiff_t base_offset = savestack(L, base);
    	
}

/* A benchmarks/spectralnorm/pallene.pln:7 */
static lua_Number function_02(
    lua_State *L,
    Udata *G,
    StackValue *base,
    lua_Integer x1, /* i */
    lua_Integer x2  /* j */
) {
    ptrdiff_t base_offset = savestack(L, base);
    	
    lua_Integer x3; /* ij */
    lua_Number x4;
    lua_Number x5;
    lua_Number x6;
    lua_Number x7;
    lua_Integer x8;
    lua_Integer x9;
    lua_Integer x10;
    lua_Number x11;
    	
    x3 = intop(+, x1, x2);
    x9 = intop(-, x3, 1);
    x10 = intop(-, x3, 2);
    x8 = intop(*, x9, x10);
    x7 = (lua_Number) x8;
    x6 = x7 * 0.5;
    x11 = (lua_Number) x1;
    x5 = x6 + x11;
    x4 = 1.0 / x5;
    return x4;
}

/* MultiplyAv benchmarks/spectralnorm/pallene.pln:13 */
static void function_03(
    lua_State *L,
    Udata *G,
    StackValue *base,
    lua_Integer x1, /* N */
    Table *x2, /* v */
    Table *x3  /* out */
) {
    ptrdiff_t base_offset = savestack(L, base);
    	
    lua_Integer x4; /* i */
    lua_Number x5; /* s */
    lua_Integer x6; /* j */
    lua_Number x7;
    lua_Number x8;
    lua_Number x9;
    	
    PALLENE_INT_FOR_LOOP_BEGIN(x4, 1, x1, 1)
    {
        x5 = 0.0;
        PALLENE_INT_FOR_LOOP_BEGIN(x6, 1, x1, 1)
        {
            x8 = function_02(L, G, base + 0, x4, x6);
            base = restorestack(L, base_offset);
            {
                pallene_renormalize_array(L, x2, x6, 17);
                TValue *slot = &x2->array[x6 - 1];
                if (PALLENE_UNLIKELY(!ttisfloat(slot))) {
                    pallene_runtime_tag_check_error(L,
                        17, LUA_VNUMFLT, rawtt(slot),
                        "array element");
                }
                x9 = fltvalue(slot);
            }
            x7 = x8 * x9;
            x5 = x5 + x7;
        }
        PALLENE_INT_FOR_LOOP_END
        {
            pallene_renormalize_array(L, x3, x4, 19);
            TValue *slot = &x3->array[x4 - 1];
            setfltvalue(slot, x5);
        }
    }
    PALLENE_INT_FOR_LOOP_END
}

/* MultiplyAtv benchmarks/spectralnorm/pallene.pln:24 */
static void function_04(
    lua_State *L,
    Udata *G,
    StackValue *base,
    lua_Integer x1, /* N */
    Table *x2, /* v */
    Table *x3  /* out */
) {
    ptrdiff_t base_offset = savestack(L, base);
    	
    lua_Integer x4; /* i */
    lua_Number x5; /* s */
    lua_Integer x6; /* j */
    lua_Number x7;
    lua_Number x8;
    lua_Number x9;
    	
    PALLENE_INT_FOR_LOOP_BEGIN(x4, 1, x1, 1)
    {
        x5 = 0.0;
        PALLENE_INT_FOR_LOOP_BEGIN(x6, 1, x1, 1)
        {
            x8 = function_02(L, G, base + 0, x6, x4);
            base = restorestack(L, base_offset);
            {
                pallene_renormalize_array(L, x2, x6, 28);
                TValue *slot = &x2->array[x6 - 1];
                if (PALLENE_UNLIKELY(!ttisfloat(slot))) {
                    pallene_runtime_tag_check_error(L,
                        28, LUA_VNUMFLT, rawtt(slot),
                        "array element");
                }
                x9 = fltvalue(slot);
            }
            x7 = x8 * x9;
            x5 = x5 + x7;
        }
        PALLENE_INT_FOR_LOOP_END
        {
            pallene_renormalize_array(L, x3, x4, 30);
            TValue *slot = &x3->array[x4 - 1];
            setfltvalue(slot, x5);
        }
    }
    PALLENE_INT_FOR_LOOP_END
}

/* MultiplyAtAv benchmarks/spectralnorm/pallene.pln:35 */
static void function_05(
    lua_State *L,
    Udata *G,
    StackValue *base,
    lua_Integer x1, /* N */
    Table *x2, /* v */
    Table *x3  /* out */
) {
    luaD_checkstack(L, 1);
    ptrdiff_t base_offset = savestack(L, base);
    	
    Table *x4; /* u */
    	
    x4 = pallene_createtable(L, 0, 0);
    sethvalue(L, s2v(base + 0), x4);
    luaC_condGC(L, L->top = base + 1, (void)0);
    function_03(L, G, base + 1, x1, x2, x4);
    base = restorestack(L, base_offset);
    function_04(L, G, base + 1, x1, x4, x3);
    base = restorestack(L, base_offset);
}

/* Approximate benchmarks/spectralnorm/pallene.pln:41 */
static lua_Number function_06(
    lua_State *L,
    Udata *G,
    StackValue *base,
    lua_Integer x1  /* N */
) {
    luaD_checkstack(L, 2);
    ptrdiff_t base_offset = savestack(L, base);
    	
    Table *x2; /* u */
    lua_Integer x3; /* i */
    Table *x4; /* v */
    lua_Integer x5; /* _ */
    lua_Number x6; /* vBv */
    lua_Number x7; /* vv */
    lua_Integer x8; /* i */
    lua_Number x9; /* ui */
    lua_Number x10; /* vi */
    lua_Number x11;
    lua_Number x12;
    lua_Number x13;
    lua_Number x14;
    	
    x2 = pallene_createtable(L, 0, 0);
    sethvalue(L, s2v(base + 0), x2);
    luaC_condGC(L, L->top = base + 1, (void)0);
    PALLENE_INT_FOR_LOOP_BEGIN(x3, 1, x1, 1)
    {
        {
            pallene_renormalize_array(L, x2, x3, 45);
            TValue *slot = &x2->array[x3 - 1];
            setfltvalue(slot, 1.0);
        }
    }
    PALLENE_INT_FOR_LOOP_END
    x4 = pallene_createtable(L, 0, 0);
    sethvalue(L, s2v(base + 1), x4);
    luaC_condGC(L, L->top = base + 2, (void)0);
    PALLENE_INT_FOR_LOOP_BEGIN(x5, 1, 10, 1)
    {
        function_05(L, G, base + 2, x1, x2, x4);
        base = restorestack(L, base_offset);
        function_05(L, G, base + 2, x1, x4, x2);
        base = restorestack(L, base_offset);
    }
    PALLENE_INT_FOR_LOOP_END
    x6 = 0.0;
    x7 = 0.0;
    PALLENE_INT_FOR_LOOP_BEGIN(x8, 1, x1, 1)
    {
        {
            pallene_renormalize_array(L, x2, x8, 58);
            TValue *slot = &x2->array[x8 - 1];
            if (PALLENE_UNLIKELY(!ttisfloat(slot))) {
                pallene_runtime_tag_check_error(L,
                    58, LUA_VNUMFLT, rawtt(slot),
                    "array element");
            }
            x9 = fltvalue(slot);
        }
        {
            pallene_renormalize_array(L, x4, x8, 59);
            TValue *slot = &x4->array[x8 - 1];
            if (PALLENE_UNLIKELY(!ttisfloat(slot))) {
                pallene_runtime_tag_check_error(L,
                    59, LUA_VNUMFLT, rawtt(slot),
                    "array element");
            }
            x10 = fltvalue(slot);
        }
        x11 = x9 * x10;
        x6 = x6 + x11;
        x12 = x10 * x10;
        x7 = x7 + x12;
    }
    PALLENE_INT_FOR_LOOP_END
    x14 = x6 / x7;
    x13 = sqrt(x14);
    return x13;
}

/* ------- */
/* Exports */
/* ------- */

static int function_01_lua(lua_State *L)
{
    StackValue *base = L->ci->func;
    CClosure *func = clCvalue(s2v(base));
    Udata *G = uvalue(&func->upvalue[0]);
    	
    int nargs = lua_gettop(L);
    if (PALLENE_UNLIKELY(nargs != 0)) {
        pallene_runtime_arity_error(L, "$init", 0, nargs);
    }
    	
    function_01(L, G, L->top);
    return 0;
}

static int function_06_lua(lua_State *L)
{
    StackValue *base = L->ci->func;
    CClosure *func = clCvalue(s2v(base));
    Udata *G = uvalue(&func->upvalue[0]);
    	
    int nargs = lua_gettop(L);
    if (PALLENE_UNLIKELY(nargs != 1)) {
        pallene_runtime_arity_error(L, "Approximate", 1, nargs);
    }
    	
    lua_Integer x1;
    	
    if (PALLENE_UNLIKELY(!ttisinteger(s2v(base + 1)))) {
        pallene_runtime_tag_check_error(L,
            41, LUA_VNUMINT, rawtt(s2v(base + 1)),
            "argument '%s'", "N");
    }
    x1 = ivalue(s2v(base + 1));
    	
    lua_Number ret1;
    ret1 = function_06(L, G, L->top, x1);
    setfltvalue(s2v(L->top), ret1);
    L->top++;
    return 1;
}

int luaopen_benchmarks_spectralnorm_pallene_mod(lua_State *L)
{
    luaL_checkversion(L);
    	
    /* Constants */
    	
    lua_newuserdatauv(L, 0, 1);
    int globals = lua_gettop(L);
    	
    lua_pushvalue(L, globals);
    lua_pushcclosure(L, function_06_lua, 1);
    lua_setiuservalue(L, globals, 1);
    	
    /* Variables & Initializers */
    	
    lua_pushvalue(L, globals);
    lua_pushcclosure(L, function_01_lua, 1);
    lua_call(L, 0, 0);
    	
    /* Exports */
    	
    lua_newtable(L);
    int export_table = lua_gettop(L);
    	
    lua_pushstring(L, "Approximate");
    lua_getiuservalue(L, globals, 1);
    lua_settable(L, export_table);
    	
    lua_pushvalue(L, export_table);
    return 1;
}

