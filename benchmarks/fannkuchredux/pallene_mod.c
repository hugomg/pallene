/* This file was generated by the Pallene compiler. Do not edit by hand" */
/* Indentation and formatting courtesy of pallene/C.lua */

#include "pallene_core.h"

/* ------- */
/* Records */
/* ------- */

/* ------------------- */
/* Function Prototypes */
/* ------------------- */

static void function_01(
    lua_State *L,
    Udata *G,
    StackValue *base
);
static Table * function_02(
    lua_State *L,
    Udata *G,
    StackValue *base,
    lua_Integer x1  /* N */
);

/* ------------------------ */
/* Function Implementations */
/* ------------------------ */

/* $init */
static void function_01(
    lua_State *L,
    Udata *G,
    StackValue *base
) {
    ptrdiff_t base_offset = savestack(L, base);
    	
}

/* fannkuch benchmarks/fannkuchredux/pallene.pln:1 */
static Table * function_02(
    lua_State *L,
    Udata *G,
    StackValue *base,
    lua_Integer x1  /* N */
) {
    luaD_checkstack(L, 4);
    ptrdiff_t base_offset = savestack(L, base);
    	
    Table *x2; /* initial_perm */
    lua_Integer x3; /* i */
    Table *x4; /* perm */
    Table *x5; /* count */
    lua_Integer x6; /* r */
    lua_Integer x7; /* perm_count */
    lua_Integer x8; /* max_flips */
    lua_Integer x9; /* checksum */
    lua_Integer x10; /* i */
    lua_Integer x11;
    lua_Integer x12; /* flips_count */
    lua_Integer x13; /* h */
    int x14;
    lua_Integer x15; /* i */
    lua_Integer x16; /* j */
    lua_Integer x17; /* a */
    lua_Integer x18; /* b */
    int x19;
    int x20;
    int x21;
    lua_Integer x22;
    int x23;
    int x24;
    Table *x25;
    lua_Integer x26; /* tmp */
    lua_Integer x27; /* i */
    lua_Integer x28;
    lua_Integer x29;
    lua_Integer x30;
    lua_Integer x31; /* r1 */
    lua_Integer x32;
    lua_Integer x33;
    int x34;
    lua_Integer x35;
    	
    x2 = pallene_createtable(L, 0, 0);
    sethvalue(L, s2v(base + 0), x2);
    luaC_condGC(L, L->top = base + 1, (void)0);
    PALLENE_INT_FOR_LOOP_BEGIN(x3, 1, x1, 1)
    {
        {
            pallene_renormalize_array(L, x2, x3, 5);
            TValue *slot = &x2->array[x3 - 1];
            setivalue(slot, x3);
        }
    }
    PALLENE_INT_FOR_LOOP_END
    x4 = pallene_createtable(L, 0, 0);
    sethvalue(L, s2v(base + 1), x4);
    luaC_condGC(L, L->top = base + 2, (void)0);
    x5 = pallene_createtable(L, 0, 0);
    sethvalue(L, s2v(base + 2), x5);
    luaC_condGC(L, L->top = base + 3, (void)0);
    {
        pallene_renormalize_array(L, x5, 1, 11);
        TValue *slot = &x5->array[1 - 1];
        setivalue(slot, 0);
    }
    x6 = x1;
    x7 = 0;
    x8 = 0;
    x9 = 0;
    while (1) {
        PALLENE_INT_FOR_LOOP_BEGIN(x10, 1, x1, 1)
        {
            {
                pallene_renormalize_array(L, x2, x10, 24);
                TValue *slot = &x2->array[x10 - 1];
                if (PALLENE_UNLIKELY(!ttisinteger(slot))) {
                    pallene_runtime_tag_check_error(L,
                        24, LUA_VNUMINT, rawtt(slot),
                        "array element");
                }
                x11 = ivalue(slot);
            }
            {
                pallene_renormalize_array(L, x4, x10, 24);
                TValue *slot = &x4->array[x10 - 1];
                setivalue(slot, x11);
            }
        }
        PALLENE_INT_FOR_LOOP_END
        x12 = 0;
        {
            pallene_renormalize_array(L, x4, 1, 28);
            TValue *slot = &x4->array[1 - 1];
            if (PALLENE_UNLIKELY(!ttisinteger(slot))) {
                pallene_runtime_tag_check_error(L,
                    28, LUA_VNUMINT, rawtt(slot),
                    "array element");
            }
            x13 = ivalue(slot);
        }
        while (1) {
            x14 = (x13 > 1);
            if (!x14) {
                break;
            }
            x15 = 1;
            x16 = x13;
            while (1) {
                {
                    pallene_renormalize_array(L, x4, x15, 33);
                    TValue *slot = &x4->array[x15 - 1];
                    if (PALLENE_UNLIKELY(!ttisinteger(slot))) {
                        pallene_runtime_tag_check_error(L,
                            33, LUA_VNUMINT, rawtt(slot),
                            "array element");
                    }
                    x17 = ivalue(slot);
                }
                {
                    pallene_renormalize_array(L, x4, x16, 34);
                    TValue *slot = &x4->array[x16 - 1];
                    if (PALLENE_UNLIKELY(!ttisinteger(slot))) {
                        pallene_runtime_tag_check_error(L,
                            34, LUA_VNUMINT, rawtt(slot),
                            "array element");
                    }
                    x18 = ivalue(slot);
                }
                {
                    pallene_renormalize_array(L, x4, x15, 35);
                    TValue *slot = &x4->array[x15 - 1];
                    setivalue(slot, x18);
                }
                {
                    pallene_renormalize_array(L, x4, x16, 36);
                    TValue *slot = &x4->array[x16 - 1];
                    setivalue(slot, x17);
                }
                x15 = intop(+, x15, 1);
                x16 = intop(-, x16, 1);
                x19 = (x15 >= x16);
                if (x19) {
                    break;
                }
            }
            x12 = intop(+, x12, 1);
            {
                pallene_renormalize_array(L, x4, 1, 42);
                TValue *slot = &x4->array[1 - 1];
                if (PALLENE_UNLIKELY(!ttisinteger(slot))) {
                    pallene_runtime_tag_check_error(L,
                        42, LUA_VNUMINT, rawtt(slot),
                        "array element");
                }
                x13 = ivalue(slot);
            }
        }
        x20 = (x12 > x8);
        if (x20) {
            x8 = x12;
        }
        x22 = pallene_int_modi(L, x7, 2, 49);
        x21 = (x22 == 0);
        if (x21) {
            x9 = intop(+, x9, x12);
        } else {
            x9 = intop(-, x9, x12);
        }
        while (1) {
            x23 = (x6 > 1);
            if (!x23) {
                break;
            }
            {
                pallene_renormalize_array(L, x5, x6, 59);
                TValue *slot = &x5->array[x6 - 1];
                setivalue(slot, x6);
            }
            x6 = intop(-, x6, 1);
        }
        while (1) {
            x24 = (x6 == x1);
            if (x24) {
                x25 = pallene_createtable(L, 2, 0);
                sethvalue(L, s2v(base + 3), x25);
                luaC_condGC(L, L->top = base + 4, (void)0);
                {
                    pallene_renormalize_array(L, x25, 1, 65);
                    TValue *slot = &x25->array[1 - 1];
                    setivalue(slot, x9);
                }
                {
                    pallene_renormalize_array(L, x25, 2, 65);
                    TValue *slot = &x25->array[2 - 1];
                    setivalue(slot, x8);
                }
                return x25;
            }
            {
                pallene_renormalize_array(L, x2, 1, 68);
                TValue *slot = &x2->array[1 - 1];
                if (PALLENE_UNLIKELY(!ttisinteger(slot))) {
                    pallene_runtime_tag_check_error(L,
                        68, LUA_VNUMINT, rawtt(slot),
                        "array element");
                }
                x26 = ivalue(slot);
            }
            PALLENE_INT_FOR_LOOP_BEGIN(x27, 1, x6, 1)
            {
                x29 = intop(+, x27, 1);
                {
                    pallene_renormalize_array(L, x2, x29, 70);
                    TValue *slot = &x2->array[x29 - 1];
                    if (PALLENE_UNLIKELY(!ttisinteger(slot))) {
                        pallene_runtime_tag_check_error(L,
                            70, LUA_VNUMINT, rawtt(slot),
                            "array element");
                    }
                    x28 = ivalue(slot);
                }
                {
                    pallene_renormalize_array(L, x2, x27, 70);
                    TValue *slot = &x2->array[x27 - 1];
                    setivalue(slot, x28);
                }
            }
            PALLENE_INT_FOR_LOOP_END
            x30 = intop(+, x6, 1);
            {
                pallene_renormalize_array(L, x2, x30, 72);
                TValue *slot = &x2->array[x30 - 1];
                setivalue(slot, x26);
            }
            x31 = intop(+, x6, 1);
            {
                pallene_renormalize_array(L, x5, x31, 75);
                TValue *slot = &x5->array[x31 - 1];
                if (PALLENE_UNLIKELY(!ttisinteger(slot))) {
                    pallene_runtime_tag_check_error(L,
                        75, LUA_VNUMINT, rawtt(slot),
                        "array element");
                }
                x33 = ivalue(slot);
            }
            x32 = intop(-, x33, 1);
            {
                pallene_renormalize_array(L, x5, x31, 75);
                TValue *slot = &x5->array[x31 - 1];
                setivalue(slot, x32);
            }
            {
                pallene_renormalize_array(L, x5, x31, 76);
                TValue *slot = &x5->array[x31 - 1];
                if (PALLENE_UNLIKELY(!ttisinteger(slot))) {
                    pallene_runtime_tag_check_error(L,
                        76, LUA_VNUMINT, rawtt(slot),
                        "array element");
                }
                x35 = ivalue(slot);
            }
            x34 = (x35 > 0);
            if (x34) {
                break;
            }
            x6 = x31;
        }
        x7 = intop(+, x7, 1);
    }
}

/* ------- */
/* Exports */
/* ------- */

static int function_01_lua(lua_State *L)
{
    StackValue *base = L->ci->func;
    CClosure *func = clCvalue(s2v(base));
    Udata *G = uvalue(&func->upvalue[0]);
    	
    int nargs = lua_gettop(L);
    if (PALLENE_UNLIKELY(nargs != 0)) {
        pallene_runtime_arity_error(L, "$init", 0, nargs);
    }
    	
    function_01(L, G, L->top);
    return 0;
}

static int function_02_lua(lua_State *L)
{
    StackValue *base = L->ci->func;
    CClosure *func = clCvalue(s2v(base));
    Udata *G = uvalue(&func->upvalue[0]);
    	
    int nargs = lua_gettop(L);
    if (PALLENE_UNLIKELY(nargs != 1)) {
        pallene_runtime_arity_error(L, "fannkuch", 1, nargs);
    }
    	
    lua_Integer x1;
    	
    if (PALLENE_UNLIKELY(!ttisinteger(s2v(base + 1)))) {
        pallene_runtime_tag_check_error(L,
            1, LUA_VNUMINT, rawtt(s2v(base + 1)),
            "argument '%s'", "N");
    }
    x1 = ivalue(s2v(base + 1));
    	
    Table *ret1;
    ret1 = function_02(L, G, L->top, x1);
    sethvalue(L, s2v(L->top), ret1);
    L->top++;
    return 1;
}

int luaopen_benchmarks_fannkuchredux_pallene_mod(lua_State *L)
{
    luaL_checkversion(L);
    	
    /* Constants */
    	
    lua_newuserdatauv(L, 0, 1);
    int globals = lua_gettop(L);
    	
    lua_pushvalue(L, globals);
    lua_pushcclosure(L, function_02_lua, 1);
    lua_setiuservalue(L, globals, 1);
    	
    /* Variables & Initializers */
    	
    lua_pushvalue(L, globals);
    lua_pushcclosure(L, function_01_lua, 1);
    lua_call(L, 0, 0);
    	
    /* Exports */
    	
    lua_newtable(L);
    int export_table = lua_gettop(L);
    	
    lua_pushstring(L, "fannkuch");
    lua_getiuservalue(L, globals, 1);
    lua_settable(L, export_table);
    	
    lua_pushvalue(L, export_table);
    return 1;
}

