/* This file was generated by the Pallene compiler. Do not edit by hand" */
/* Indentation and formatting courtesy of pallene/C.lua */

#include "pallene_core.h"

/* ------- */
/* Records */
/* ------- */

/* ------------------- */
/* Function Prototypes */
/* ------------------- */

static void function_01(
    lua_State *L,
    Udata *G,
    StackValue *base
);
static lua_Number function_02(
    lua_State *L,
    Udata *G,
    StackValue *base,
    Table *x1, /* A */
    Table *x2  /* B */
);

/* ------------------------ */
/* Function Implementations */
/* ------------------------ */

/* $init */
static void function_01(
    lua_State *L,
    Udata *G,
    StackValue *base
) {
    	
}

/* matmul benchmarks/matmulsum/pallene.pln:1 */
static lua_Number function_02(
    lua_State *L,
    Udata *G,
    StackValue *base,
    Table *x1, /* A */
    Table *x2  /* B */
) {
    lua_Number x3; /* s */
    lua_Integer x4; /* NI */
    lua_Integer x5; /* NK */
    lua_Integer x6; /* NJ */
    lua_Integer x7; /* k */
    Table *x8; /* Bk */
    lua_Integer x9; /* i */
    lua_Number x10; /* Aik */
    lua_Integer x11; /* j */
    Table *x12;
    Table *x13;
    lua_Number x14;
    lua_Number x15;
    	
    x3 = 0.0;
    if (x1->metatable) {
        pallene_runtime_array_metatable_error(L, 3);
    }
    x4 = luaH_getn(x1);
    if (x2->metatable) {
        pallene_runtime_array_metatable_error(L, 4);
    }
    x5 = luaH_getn(x2);
    {
        pallene_renormalize_array(L, x2, 1, 5);
        TValue *slot = &x2->array[1 - 1];
        if (PALLENE_UNLIKELY(!ttistable(slot))) {
            pallene_runtime_tag_check_error(L,
                5, LUA_TTABLE, rawtt(slot),
                "array element");
        }
        x12 = hvalue(slot);
    }
    if (x12->metatable) {
        pallene_runtime_array_metatable_error(L, 5);
    }
    x6 = luaH_getn(x12);
    // for hoist
    pallene_renormalize_array(L, x2, x5, 7);
    // for hoist
    pallene_renormalize_array(L, x1 , x4, 9);
    PALLENE_INT_FOR_LOOP_BEGIN(x7, 1, x5, 1)
    {
        {
            TValue *slot = &x2->array[x7 - 1];
            if (PALLENE_UNLIKELY(!ttistable(slot))) {
                pallene_runtime_tag_check_error(L,
                    7, LUA_TTABLE, rawtt(slot),
                    "array element");
            }
            x8 = hvalue(slot);
        }
        // for hoist
        pallene_renormalize_array(L, x8, x6, 11);
        PALLENE_INT_FOR_LOOP_BEGIN(x9, 1, x4, 1)
        {
            {
                
                TValue *slot = &x1->array[x9 - 1];
                if (PALLENE_UNLIKELY(!ttistable(slot))) {
                    pallene_runtime_tag_check_error(L,
                        9, LUA_TTABLE, rawtt(slot),
                        "array element");
                }
                x13 = hvalue(slot);
            }
            {
                pallene_renormalize_array(L, x13, x7, 9);
                TValue *slot = &x13->array[x7 - 1];
                if (PALLENE_UNLIKELY(!ttisfloat(slot))) {
                    pallene_runtime_tag_check_error(L,
                        9, LUA_TNUMFLT, rawtt(slot),
                        "array element");
                }
                x10 = fltvalue(slot);
            }
            
            PALLENE_INT_FOR_LOOP_BEGIN(x11, 1, x6, 1)
            {
                {
                    
                    TValue *slot = &x8->array[x11 - 1];
                    if (PALLENE_UNLIKELY(!ttisfloat(slot))) {
                        pallene_runtime_tag_check_error(L,
                            11, LUA_TNUMFLT, rawtt(slot),
                            "array element");
                    }
                    x15 = fltvalue(slot);
                }
                x14 = x10 * x15;
                x3 = x3 + x14;
            }
            PALLENE_INT_FOR_LOOP_END
        }
        PALLENE_INT_FOR_LOOP_END
    }
    PALLENE_INT_FOR_LOOP_END
    return x3;
}

/* ------- */
/* Exports */
/* ------- */

static int function_01_lua(lua_State *L)
{
    StackValue *base = L->ci->func;
    CClosure *func = clCvalue(s2v(base));
    Udata *G = uvalue(&func->upvalue[0]);
    	
    int nargs = lua_gettop(L);
    if (PALLENE_UNLIKELY(nargs != 0)) {
        pallene_runtime_arity_error(L, "$init", 0, nargs);
    }
    	
    function_01(L, G, L->top);
    return 0;
}

static int function_02_lua(lua_State *L)
{
    StackValue *base = L->ci->func;
    CClosure *func = clCvalue(s2v(base));
    Udata *G = uvalue(&func->upvalue[0]);
    	
    int nargs = lua_gettop(L);
    if (PALLENE_UNLIKELY(nargs != 2)) {
        pallene_runtime_arity_error(L, "matmul", 2, nargs);
    }
    	
    Table *x1;
    Table *x2;
    	
    if (PALLENE_UNLIKELY(!ttistable(s2v(base + 1)))) {
        pallene_runtime_tag_check_error(L,
            1, LUA_TTABLE, rawtt(s2v(base + 1)),
            "argument '%s'", "A");
    }
    x1 = hvalue(s2v(base + 1));
    	
    if (PALLENE_UNLIKELY(!ttistable(s2v(base + 2)))) {
        pallene_runtime_tag_check_error(L,
            1, LUA_TTABLE, rawtt(s2v(base + 2)),
            "argument '%s'", "B");
    }
    x2 = hvalue(s2v(base + 2));
    	
    lua_Number ret1;
    ret1 = function_02(L, G, L->top, x1, x2);
    setfltvalue(s2v(L->top), ret1);
    L->top++;
    return 1;
}

int luaopen_benchmarks_matmulsum_pallene_mod(lua_State *L)
{
    luaL_checkversion(L);
    	
    lua_newuserdatauv(L, 0, 0);
    int globals = lua_gettop(L);
    	
    lua_createtable(L, 2, 0);
    int closures = lua_gettop(L);
    	
    lua_newtable(L);
    int export_table = lua_gettop(L);
    	
    /* Closures */
    	
    lua_pushvalue(L, globals);
    lua_pushcclosure(L, function_01_lua, 1);
    lua_seti(L, closures, 1);
    	
    lua_pushvalue(L, globals);
    lua_pushcclosure(L, function_02_lua, 1);
    lua_seti(L, closures, 2);
    	
    /* Global values */
    	
    // Run toplevel statements & initialize globals
    lua_geti(L, closures, 1);
    lua_call(L, 0, 0);
    	
    /* Exports */
    	
    lua_pushstring(L, "matmul");
    lua_geti(L, closures, 2);
    lua_settable(L, export_table);
    	
    lua_pushvalue(L, export_table);
    return 1;
}

